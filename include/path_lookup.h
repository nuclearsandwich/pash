// objectives:
//char* resolve_data(char*)
//char* resolve_executable(char*)

//a collection of functions to resolve path variables


// tests whether or not 
char* resolve_data(char* inputPath)
{
  

}

// tests whether or not an executable is there, if yes, return complete path, else return NULL
char* resolve_executable(char* inputPath)
{
  

}

// there is a path variable that holds directories. search those path entries for the executable and return the fully qualified path
